print("hello")
setwd('E:\Academics\VIT\Lab or Practicals\Data Analytics\Lab4')
setwd('E:\\Academics\\VIT\\Lab or Practicals\\Data Analytics\\Lab4')
cards<-read.csv("deck.csv")
data
cards<-read.csv("deck.csv")
cards
cards<-read.csv("deck.csv")
print('1. Draw a card\n2. Shuffle')
cards
cards<-read.csv("deck.csv")
print('1. Draw a card')
print('2. Shuffle')
cards
cards<-read.csv("deck.csv")
print('1. Draw a card')
print('2. Shuffle')
cards[sample(nrow(cards),2)]
cards<-read.csv("deck.csv")
print('1. Draw a card')
print('2. Shuffle')
cards[sample(nrow(cards),2),]
cards<-read.csv("deck.csv")
print('1. Draw a card')
print('2. Shuffle')
cards[sample(nrow(cards),2),]
cards<-read.csv("deck.csv")
print('1. Draw a card')
print('2. Shuffle')
cards[sample(nrow(cards),2),]
cards<-read.csv("deck.csv")
print("1. Draw a car\nd")
print('2. Shuffle')
cards[sample(nrow(cards),2),]
deck<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck)<-c("face","suit","value")
deck
cards<-read.csv("deck.csv")
print("1. Draw a card")
print('2. Shuffle')
drawn<-cards[sample(nrow(cards),1),]
deck<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck)<-c("face","suit","value")
deck<-rbind(deck,drawn)
deck
cards<-read.csv("deck.csv")
print("1. Draw a card")
print('2. Shuffle')
drawn<-cards[sample(nrow(cards),1),]
deck<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck)<-c("face","suit","value")
deck<-rbind(deck,drawn)
deck
cards<-cards[-drawn]
cards
ind<-sample(nrow(cards),1)
drawn<-cards[ind,]
ind
#draw a random card
ind<-sample(nrow(cards),1)
drawn<-cards[ind,]
ind
drawn
cards<-read.csv("deck.csv")
#draw a random card
ind<-sample(nrow(cards),1)
drawn<-cards[ind,]
ind
drawn
deck<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck)<-c("face","suit","value")
#put that card on deck
deck<-rbind(deck,drawn)
deck
cards<-cards[-ind]
cards
cards<-read.csv("deck.csv")
#draw a random card
ind<-sample(nrow(cards),1)
drawn<-cards[ind,]
ind
drawn
deck<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck)<-c("face","suit","value")
#put that card on deck
deck<-rbind(deck,drawn)
deck
cards<-cards[-c(ind)]
cards
cards<-read.csv("deck.csv")
#draw a random card
ind<-sample(nrow(cards),1)
drawn<-cards[ind,]
ind
drawn
deck<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck)<-c("face","suit","value")
#put that card on deck
deck<-rbind(deck,drawn)
deck
cards<-cards[-c(ind),]
cards
deck<-read.csv("deck.csv")
#draw a random card
ind<-sample(nrow(deck),1)
drawn<-deck[ind,]
ind
drawn
deck2<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck2)<-c("face","suit","value")
#put that card on deck2
deck2<-rbind(deck,drawn)
deck2
#remove that card from deck1
deck<-deck[-c(ind),]
deck
deck<-read.csv("deck.csv")
#draw a random card
ind<-sample(nrow(deck),1)
drawn<-deck[ind,]
ind
drawn
deck2<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck2)<-c("face","suit","value")
#put that card on deck2
deck2<-rbind(deck,drawn)
deck2
#remove that card from deck1
deck<-deck[-c(ind),]
deck
deck<-read.csv("deck.csv")
#draw a random card
ind<-sample(nrow(deck),1)
drawn<-deck[ind,]
ind
drawn
deck2<-data.frame(matrix(ncol=3,nrow=0))
colnames(deck2)<-c("face","suit","value")
#put that card on deck2
deck2<-rbind(deck2,drawn)
deck2
#remove that card from deck1
deck<-deck[-c(ind),]
deck
